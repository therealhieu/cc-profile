# 2026-06-30-init — Execution Goal

## Persona

You are a senior implementation agent working in this repository. Follow project rules, use TDD, keep scope tight, protect user work, and make small focused commits after completed tasks.

## Context

- Required execution skill: `superpowers:subagent-driven-development` is recommended; `superpowers:executing-plans` is acceptable if the user chooses inline execution.
- Design: `docs/superpowers/active/2026-06-30-init/2026-06-30-init-design.md`.
- Plan index: `docs/superpowers/active/2026-06-30-init/2026-06-30-init-plan.md`.
- Part plans:
  - `docs/superpowers/active/2026-06-30-init/2026-06-30-init-plan-1.md`
  - `docs/superpowers/active/2026-06-30-init/2026-06-30-init-plan-2.md`
  - `docs/superpowers/active/2026-06-30-init/2026-06-30-init-plan-3.md`
  - `docs/superpowers/active/2026-06-30-init/2026-06-30-init-plan-4.md`
- Manual verification: `docs/superpowers/active/2026-06-30-init/2026-06-30-init-manual.md`.
- Post-implementation check: `docs/superpowers/active/2026-06-30-init/2026-06-30-init-check.md`.
- Standards: `docs/specs/development.md`, `docs/specs/rust.md`, `docs/specs/rust_testing.md`, and `docs/specs/git.md`.
- Goal: build a Rust `cc-profile` CLI that stores Claude Code profiles in `~/.cc-profile`, manages them interactively and through core commands, and launches `claude` with active-profile environment variables and global args.
- Architecture: keep `clap` and `dialoguer` at the edge, mutate config through services, persist config through `ConfigRepository`, and test launch behavior through `CommandSpec` before using `std::process::Command`.
- Tech stack: Rust 2021, `anyhow`, `bon`, `clap`, `dialoguer`, `dirs`, `serde`, `toml`, `assert_cmd`, `assert_fs`, `predicates`, and `cargo-nextest`.
- Implementation order is sequential by part: Part 1 config foundation → Part 2 services/launch → Part 3 non-interactive CLI → Part 4 interactive UI/final verification.
- Parallelism constraints: Part 1 Tasks 4–5 can run after Task 2 if interfaces remain stable; Part 2 Tasks 1–3 can run in parallel after Part 1; Part 3 and Part 4 are mostly sequential.
- Design constraints: use `~/.cc-profile`, store API keys in TOML for v1, show API keys as stored in normal config/profile displays, never write secrets to errors/logs/test snapshots, warn when `active_profile` points to a missing profile, use `BTreeMap`, keep `bon::Builder`, report/fix broad Unix permissions to `0600`, and preserve env precedence `global envs → profile Claude envs`.
- Non-goals: encrypted storage, keychain integration, profile-specific env vars, profile-specific args, and `ANTHROPIC_AUTH_TOKEN`.

## Tasks

- Execute `2026-06-30-init-plan.md` and its four part plans task-by-task.
- For every task: write the listed failing test first, run the specified command to verify the expected failure, implement the minimal code, run the targeted verification, run reviewer loops, mark the checkbox, then commit.
- Preserve plan-level sequencing and parallelism exactly; do not start a dependent task before its producer task is complete.
- Use fresh subagents with full task context if using `superpowers:subagent-driven-development`.
- Run `spec-reviewer` and `code-quality-reviewer` in parallel after each task implementation and test pass.
- If implementation must deviate from a planned interface, stop and document the reason before continuing.
- Run every manual flow in `2026-06-30-init-manual.md` after automated verification.
- Fill `2026-06-30-init-check.md` only from concrete implementation, verification, PR, and CI results.

## Success Criteria

- Task completion: Part 1 Tasks 1–5, Part 2 Tasks 1–4, Part 3 Tasks 1–5, and Part 4 Tasks 1–4 are complete.
- Implementation matches the design and plan with no extra scope.
- `cargo fmt --check` succeeds.
- `cargo clippy --all-targets --all-features -- -D warnings` succeeds.
- `cargo nextest run --workspace` succeeds.
- `cargo test --doc --workspace` succeeds.
- `./scripts/ci.sh` succeeds if it exists; otherwise the absence is recorded and cargo verification commands are the local CI signal.
- Manual verification covers first run, create profile, list/detail, set active, edit/rename, delete active, args toggle, env add/edit/delete, show config, start launch env precedence, missing active-profile warning, and invalid TOML recovery.
- `cc-profile start` passes `ANTHROPIC_BASE_URL`, `ANTHROPIC_API_KEY`, `ANTHROPIC_DEFAULT_FABLE_MODEL`, `ANTHROPIC_DEFAULT_OPUS_MODEL`, `ANTHROPIC_DEFAULT_SONNET_MODEL`, and `ANTHROPIC_DEFAULT_HAIKU_MODEL` from the active profile.
- `--dangerously-skip-permissions` is passed only when configured true.
- No production `todo!`, placeholders, TODO comments, or unfinished work remain.
- PR is open, linked in the check file, and all GitHub Actions workflows are green before final completion is reported.
